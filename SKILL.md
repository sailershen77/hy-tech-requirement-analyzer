---
name: hy-tech-requirement-analyzer
description: "Analyze project requirement documents (.docx) to extract technical requirements and output them in Markdown format. If the document contains a scoring/evaluation table (评分表), also output the technical scoring table portion. After analysis, also generate the technical section outline (技术部分目录结构): the outline is delivered as a .docx file where level-1/2/3 outline entries use Word built-in styles 标题 1/标题 2/标题 3 (Heading 1/2/3), ready to be used as the bid document skeleton. Use this skill when the user asks to analyze a docx requirement document, extract technical requirements, output 技术需求 as Markdown, find 评分表/评标表, or generate 技术标书目录/技术部分目录结构 (markdown or docx) from a requirement document. Triggers include: 提取技术需求, 分析需求书技术需求, 输出技术需求markdown, 技术需求分析, 提取评分表, 评标表分析, 技术标书目录, 技术部分目录结构, 技术部分目录docx, extract technical requirements, analyze technical requirements from docx."
agent_created: true
---

# Tech Requirement Analyzer - docx 技术需求提取

## Overview

本 Skill 将项目需求书（docx 格式）作为输入，提取其中的**技术需求**，输出为 Markdown 文档；若需求书中包含**评分表**，同步输出技术评分表部分；并基于评分表的技术评分项与技术需求，进一步生成**技术部分目录结构**（投标书技术部分的建议章节框架），用于投标响应或标书编制。

适用范围：软件/IT 项目需求书、采购需求书、招标文件、技术规格书等，凡含"技术需求/技术要求/技术参数"章节或评分标准的 docx 均可分析。

输出文档共三部分：
1. **技术需求清单**（分类表格）
2. **技术评分表**（若存在）
3. **技术部分目录结构**（评分项→章节骨架 + 对照表）

最终交付物：
- **Markdown 分析文档**（三部分完整内容）
- **技术部分目录结构 docx**（章节骨架文件）：一级目录应用 Word 样式"标题 1"、二级目录"标题 2"、三级要点"标题 3"，标题文本不带数字序列号（可搭配 Word 多级列表自动编号），可直接作为投标书技术部分骨架，在 Word 中生成目录、导航查看、逐章填写内容

## 前置依赖

提取脚本依赖 `python-docx` 库。若脚本报缺少依赖，先安装（优先使用国内镜像）：

```bash
pip install python-docx -i https://pypi.tuna.tsinghua.edu.cn/simple
```

## 工作流

### Step 1: 确认输入并提取 docx 内容

确认用户提供的 docx 文件路径。运行提取脚本，将文档转为结构化 JSON：

```bash
python scripts/extract_docx.py <input.docx> --output <output.json>
```

脚本输出包含按文档顺序排列的 `items` 数组，每个元素为：
- `{"kind": "paragraph", "text": "...", "style": "...", "heading_level": 1|2|3|null, "idx": N}`
- `{"kind": "table", "rows": [["...", ...], ...], "idx": N}`

若用户提供的是其他格式（PDF、文本），说明本 Skill 面向 docx，并建议用户转换格式；若用户直接粘贴文本，可跳过脚本，直接按文本分析。

### Step 2: 加载提取指南

读取参考文档，应用其中定义的技术需求识别规则、分类体系与评分表识别规则：

```
Read: references/extraction_guide.md
```

### Step 3: 定位技术需求章节

根据指南第 1.1 节的标题模式，在 items 中定位技术需求区域的起始标题（如"二、技术需求"、"技术要求"、"技术规格"）。

从该起始位置到下一顶级章节（如"评分表"、"商务要求"、"交付要求"）之间的段落与表格，均属于技术需求区域。

若文档结构不清晰（无明确标题），执行降级策略：扫描全文，提取所有含强约束词或量化指标的句子作为技术需求。

### Step 4: 提取并分类技术需求

对技术需求区域内的每个段落：

1. 判断是否含约束词（必须/应/宜/可/shall/should）或量化指标（≤/≥/TPS/秒/GB等）
2. 是 → 按指南第 2 节分类体系归类（架构/功能/性能/安全/接口/运维/其他）
3. 否 → 跳过（如说明性文字、举例）

对技术需求区域内的表格：
- 若是**参数响应表**（含列如"参数名称/指标值/响应情况"）→ 每行提取为一条技术需求
- 若是**需求一览表**（含"功能/指标/要求"列）→ 每行提取为一条技术需求

对每条需求记录：
- 需求文本（清洗：去掉编号前缀、首尾空白）
- 分类（按指南第 2 节）
- 约束程度（按指南 4.3 判定：必须/建议/可选）
- 备注（来源章节号、量化指标、重复说明）

### Step 5: 识别并提取评分表

按指南第 3 节规则在 items 中搜索评分表：

1. 查找标题含"评分表/评分标准/评标办法/评标细则/评审因素"等词的段落
2. 定位紧随其后的表格，用指南 3.2 的特征判断是否为评分表
3. 是 → 记录完整表格行数据 + 表格前说明文字
4. 无评分表 → 记录"未发现评分表"，输出时说明

注意：技术参数响应表（无"分值/得分"列）不属于评分表；商务报价表、价格明细表不输出。

### Step 6: 生成技术部分目录结构

按指南第 5 节规则生成"技术部分目录结构"，章节与评分项的映射遵循以下硬性规则（详见指南 5.1）：

1. **一级标题优先对应评分大项**（评分表中的大项/分类行，如"技术方案""实施方案"）
2. **二级标题优先对应评分小项**（评分大项下的细分项/子行，如"1.1 进度计划"对应评分细则中的进度安排要求）
3. **章节顺序尽量跟评分顺序一致**，按评分表行序编排章节，便于评委对号入座
4. **每个评分点都要有可定位的章节**——逐一核对评分表每行，确保在目录中有明确对应的章节（一级或二级），无遗漏；确实无法对应时在对照表中显式标注
5. **不确定是否需要单独成章的内容，标注【建议人工确认】**——该标注保留在标题文本末尾，Markdown 与 docx 输出均保留，供编制人复核

具体步骤：
1. 取 Step 5 识别的评分表中**技术部分评分项** → 逐一映射为一级章节骨架（指南 5.1）
2. 取 Step 4 提取的技术需求 → 按分类映射到各章节，归纳为二级标题与"内容要点"（指南 5.2）
3. 无评分表或评分表无技术项 → 使用指南 5.3 的通用骨架兜底
4. 输出目录结构（数字序列号编号） + 章节与评分项对照表（指南 5.4）

### Step 7: 生成 Markdown 输出

按指南第 4、5 节的输出规范生成 Markdown 文档，含三部分：

```markdown
# {文档标题} - 技术需求分析

> 文档信息：{项目编号} | 分析日期：{YYYY-MM-DD}

## 一、技术需求

### 1. {分类名}
| 序号 | 技术需求 | 分类 | 约束程度 | 备注 |
|:---:|:---|:---:|:---:|:---|
| 1 | {需求文本} | {分类} | {必须/建议/可选} | {备注} |

## 二、技术评分表

> {评分方法说明（如有）}

| 序号 | 评分项 | 分值 | 评分标准 |
|:---:|:---|:---:|:---|
| 1 | {评分项} | {分值} | {评分标准} |

## 三、技术部分目录结构

> {生成依据说明}

### 1. 目录结构

1. 实施方案　〔20 分〕
   1.1 项目概述与理解
   1.2 ...
   ...

### 2. 章节与评分项对照表

| 章节 | 对应评分项 | 分值 | 编制要点 |
|:---|:---|:---:|:---|
| ... |
```

要求：
- 分类章节按 2.1→2.7 顺序输出，无内容的分类不出现
- 技术需求表与评分表均为 Markdown 表格格式
- 未发现评分表时，第二部分输出：`未在需求书中发现评分表。`，第三部分用通用骨架（指南 5.3）
- 完整保留评分表的行列结构与单元格内容
- 第三部分目录结构：章节与评分项对应、分值标注 `〔N 分〕`、内容要点源自技术需求原文
- 目录结构编号规则：一级用 `1.`、`2.`、`3.`…，二级用 `1.1`、`1.2`、`2.1`、`2.2`…，**不使用 `├──`/`└──`/`│` 等树形符号，也不使用"一、二、三"中文序号**

### Step 8: 生成目录结构 docx 并保存展示

1. 将 Markdown 分析文档保存到工作区输出目录：

```
{workspace}/outputs/technical-requirements-{日期}.md
```

2. 将第三部分"目录结构"转换为 docx 章节骨架（脚本自动识别 markdown 中的层级列表行，其他行跳过）：

```bash
python scripts/outline_to_docx.py <analysis.md> <output.docx> --title "{项目名} 技术部分目录结构"
```

docx 样式要求（硬性）：
- 一级目录（如 `1. 服务方案　〔20 分〕`）→ 样式 **"标题 1"**（Heading 1），标题文本为 `服务方案　〔20 分〕`
- 二级目录（如 `1.1 项目理解与重难点分析`）→ 样式 **"标题 2"**（Heading 2），标题文本为 `项目理解与重难点分析`
- 三级要点（如 `评估目标（...）`）→ 样式 **"标题 3"**（Heading 3）
- **数字序列号（`1.` / `1.1` / `1.1.1`）在 docx 标题文本中一律去掉**（脚本自动剥离），只保留标题文字、标题样式、分值标注 `〔N 分〕` 与【建议人工确认】标注；编号由 Word 多级列表/自动编号功能按标题样式生成
- **每个标题行（Title / 标题 1/2/3）下面追加一个空行**（Normal 样式空段落），便于直接填写章节内容
- **删除未使用的多余样式**：文档只保留 Normal、Title、Heading 1/2/3 五个样式（脚本自动清理），样式面板干净不干扰后续排版

docx 命名规范：

```
{workspace}/outputs/technical-part-outline-{日期}.docx
```

3. 使用 present_files 同时展示 Markdown 分析文档与 docx 目录骨架。

## 关键原则

1. **忠实原文**：技术需求与评分表内容忠实于原文，不增删、不润色、不虚构。
2. **技术需求优先**：只提取技术相关内容，商务、资格、评标办法说明等不进入技术需求清单。
3. **量化优先**：含量化指标的需求优先保留完整数值（如"响应时间不超过3秒"不可省略为"响应时间有要求"）。
4. **评分表完整性**：评分表按原结构完整输出，表头、分值、评分标准逐列保留。
5. **无表不虚构**：未发现评分表必须如实说明，不得编造评分标准。
6. **交付物形态**：Markdown 分析文档 + 技术部分目录结构 docx，不生成 HTML 报告。
7. **目录对应评分项**：技术部分目录以技术评分项为骨架，每个技术评分项必须有对应章节并标注分值，不得遗漏。
8. **要点源自需求**：目录中章节"内容要点"来自提取的技术需求原文，不虚构章节内容。
9. **目录数字编号**：目录结构一级用 `1.`、`2.`，二级用 `1.1`、`1.2`，不使用 `├──`/`└──`/`│` 树形符号，不使用"一、二、三"中文序号。
10. **docx 标题样式**：目录结构 docx 中，一级目录用"标题 1"、二级目录用"标题 2"、三级要点用"标题 3"样式（Word 内置 Heading 1/2/3）；docx 标题文本中**去掉数字序列号**（脚本自动剥离 `1.`/`1.1` 等），保留标题文字与分值标注 `〔N 分〕`；每个标题行下加一个空行；文档只保留 Normal、Title、Heading 1/2/3 五个样式。Markdown 主文档中目录结构仍保留数字编号。
11. **章节映射规则**：一级标题优先对应评分大项，二级标题优先对应评分小项；章节顺序尽量跟评分顺序一致；每个评分点都要有可定位的章节；不确定是否需要单独成章的内容，标注【建议人工确认】（Markdown 与 docx 输出均保留该标注）。

## 示例

用户："分析这份需求书的技术需求" + 提供 `需求书.docx`

输出：

```markdown
# XX市智慧政务平台建设项目需求书 - 技术需求分析

> 文档信息：XX-2026-001 | 分析日期：2026-08-19

## 一、技术需求

### 1. 系统架构类

| 序号 | 技术需求 | 分类 | 约束程度 | 备注 |
|:---:|:---|:---:|:---:|:---|
| 1 | 系统应采用B/S架构，基于微服务框架开发，支持容器化部署 | 架构 | 必须 | 2.1节 |
| 2 | 技术栈应选用当前主流且成熟稳定的技术，原则上采用Java语言开发 | 架构 | 必须 | 2.1节 |

### 2. 功能需求类

| 序号 | 技术需求 | 分类 | 约束程度 | 备注 |
|:---:|:---|:---:|:---:|:---|
| 3 | 系统应提供统一身份认证功能，支持账号密码、短信验证码、CA证书等多种认证方式 | 功能 | 必须 | 2.2.1节 |
| 4 | 系统应支持政务服务事项的录入、审核、发布、下架全生命周期管理 | 功能 | 必须 | 2.2.2节 |

### 3. 性能需求类

| 序号 | 技术需求 | 分类 | 约束程度 | 备注 |
|:---:|:---|:---:|:---:|:---|
| 5 | 系统首页平均响应时间应不超过3秒；核心业务办理接口吞吐量应不低于500 TPS | 性能 | 必须 | 2.3.1节 |

### 4. 安全需求类

| 序号 | 技术需求 | 分类 | 约束程度 | 备注 |
|:---:|:---|:---:|:---:|:---|
| 6 | 系统应通过三级等保测评，数据传输应采用国密算法加密，关键操作应留有审计日志 | 安全 | 必须 | 2.3.2节 |

## 二、技术评分表

> 评标采用综合评分法，总分100分，其中技术部分65分，商务部分35分。

| 序号 | 评分项 | 分值 | 评分标准 |
|:---:|:---|:---:|:---|
| 1 | 技术方案 | 20 | 方案完整、合理、可行，满足需求书要求得20分；每有一处不满足扣2分，扣完为止 |
| 2 | 系统架构 | 15 | 架构先进合理，微服务化程度高得15分；一般得8分；较差得3分 |
| 3 | 技术参数响应 | 15 | 全部参数满足得15分；每有一项负偏离扣2分，扣完为止 |
| 4 | 项目实施 | 10 | 实施计划详细可行、人员配备合理得10分；一般得5分 |
| 5 | 售后服务 | 5 | 质保期3年得3分，每增加1年加1分，最高5分 |

## 三、技术部分目录结构

> 依据评分表技术评分项（技术方案/系统架构/技术参数响应/项目实施/售后服务）与技术需求生成，便于投标响应与评委打分。

### 1. 目录结构

1. 技术方案　〔20 分〕
   1.1 项目概述与标准理解
   1.2 总体架构与技术选型（B/S、微服务、容器化部署）
   1.3 功能需求响应（统一身份认证、事项全生命周期管理）
   1.4 技术方案风险与应对

2. 系统架构　〔15 分〕
   2.1 架构设计说明（微服务化程度）
   2.2 部署架构与信创适配

3. 技术参数响应　〔15 分〕
   3.1 性能指标响应（响应时间≤3秒、吞吐量≥500TPS）
   3.2 安全合规响应（三级等保、国密算法、审计日志）

4. 项目实施　〔10 分〕
   4.1 实施计划与进度安排
   4.2 人员配备与项目管理

5. 售后服务　〔5 分〕
   5.1 质保期与响应时效承诺

### 2. 章节与评分项对照表

| 章节 | 对应评分项 | 分值 | 编制要点 |
|:---|:---|:---:|:---|
| 1. 技术方案 | 技术方案 | 20 | 完整、合理、可行 |
| 2. 系统架构 | 系统架构 | 15 | 先进合理、微服务化程度 |
| 3. 技术参数响应 | 技术参数响应 | 15 | 全部参数满足、无负偏离 |
| 4. 项目实施 | 项目实施 | 10 | 计划详细、人员合理 |
| 5. 售后服务 | 售后服务 | 5 | 质保期年限 |
```

## Resources

### scripts/
- `extract_docx.py` — 提取 docx 的段落（含标题层级）与表格，输出结构化 JSON。运行方式见 Step 1。需 python-docx 库。
- `outline_to_docx.py` — 把"技术部分目录结构"的 Markdown 层级列表转换为 docx 章节骨架：一级目录→"标题 1"、二级目录→"标题 2"、三级要点→"标题 3"（Heading 1/2/3），自动剥离标题行开头的数字序列号，每个标题行下加空行，并删除未使用的多余样式（只留 Normal/Title/Heading 1/2/3）。运行方式见 Step 8。需 python-docx 库。

### references/
- `extraction_guide.md` — 技术需求识别规则（章节定位模式、约束词、量化指标）、7 类分类体系、评分表识别特征、技术部分目录结构生成规则（评分项→章节骨架、需求→内容要点、通用骨架、输出规范）、Markdown 输出规范与常见坑处理。分析时必须加载（Step 2）。
